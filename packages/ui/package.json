{
  "name": "@zana/ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist --sourcemap",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "storybook": "node -e \"console.log('Storybook is not configured yet.');\""
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.6.3"
  }
}
